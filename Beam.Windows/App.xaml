<Application x:Class="Beam.Windows.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Modern Color Palette (Beam Navy & Green) -->
            <Color x:Key="ColorPrimary">#25F48C</Color>
            <Color x:Key="ColorDark">#0F1923</Color>
            <Color x:Key="ColorSurface">#16202C</Color>
            <Color x:Key="ColorText">#F1F5F9</Color>
            <Color x:Key="ColorTextDim">#94A3B8</Color>
            
            <SolidColorBrush x:Key="BrushPrimary" Color="{StaticResource ColorPrimary}"/>
            <SolidColorBrush x:Key="BrushDark" Color="{StaticResource ColorDark}"/>
            <SolidColorBrush x:Key="BrushSurface" Color="{StaticResource ColorSurface}"/>
            <SolidColorBrush x:Key="BrushText" Color="{StaticResource ColorText}"/>
            <SolidColorBrush x:Key="BrushTextDim" Color="{StaticResource ColorTextDim}"/>

            <!-- Secondary/Accent Brushes -->
            <SolidColorBrush x:Key="BrushPrimaryTransparent" Color="#1A25F48C"/>
            <SolidColorBrush x:Key="BrushBorder" Color="#334155"/>
            <SolidColorBrush x:Key="BrushGlassFill" Color="#B316202C"/>
            <SolidColorBrush x:Key="BrushGlassBorder" Color="#0DFFFFFF"/>

            <local:BooleanToAlignmentConverter x:Key="BooleanToAlignmentConverter" xmlns:local="clr-namespace:Beam.Windows.Converters"/>
            <local:BooleanToColorConverter x:Key="BooleanToColorConverter" xmlns:local="clr-namespace:Beam.Windows.Converters"/>
            <local:BooleanToCornerConverter x:Key="BooleanToCornerConverter" xmlns:local="clr-namespace:Beam.Windows.Converters"/>
            <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter" xmlns:local="clr-namespace:Beam.Windows.Converters"/>
            <local:InverseStringToVisibilityConverter x:Key="InverseStringToVisibilityConverter" xmlns:local="clr-namespace:Beam.Windows.Converters"/>
            <local:ProgressToVisibilityConverter x:Key="ProgressToVisibilityConverter" xmlns:local="clr-namespace:Beam.Windows.Converters"/>

            <!-- Modern Button Style (Pill/Gradient) -->
            <Style x:Key="ModernButton" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource BrushPrimary}"/>
                <Setter Property="Foreground" Value="#0F1923"/>
                <Setter Property="Padding" Value="20,10"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="20" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#25F48C" Offset="0"/>
                                                <GradientStop Color="#1ABF6D" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Modern TextBox Style (Pill/Glass) -->
            <Style x:Key="ModernTextBox" TargetType="TextBox">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="16,12"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="SelectionBrush" Value="{StaticResource BrushPrimary}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="22">
                                <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
